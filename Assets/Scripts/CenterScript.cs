using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CenterScript : MonoBehaviour
{
    /// <summary>
    /// Ture if the objects are ready for capture
    /// </summary>
    [Tooltip("Ture if the objects are ready for capture.")]
    public bool status = false;

}
